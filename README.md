# File-Organizer


# Ru

Скрипт при открытие в каталоге в котором он был запущен смотроит список всех файлов и сортирует их по папкам

# EN

The script, when opened in the directory in which it was launched, looks at the list of all files and sorts them into folders.
